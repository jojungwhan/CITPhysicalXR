# Risk Register

Status: active. Likelihood and impact use Low/Medium/High.

| ID    | Risk                                                                         | Likelihood | Impact   | Control / mitigation                                                                                                              | Exit evidence                                         |
| ----- | ---------------------------------------------------------------------------- | ---------- | -------- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| R-001 | Private or unlicensed source is copied into Apache-2.0 core                  | Medium     | High     | API/process wrappers only; preserve provenance; owner/licence review before package reuse                                         | Licence check, diff review, third-party notices       |
| R-002 | Existing G2/Meta prompt or completion behavior regresses                     | Medium     | High     | Opt-in Fabric flag default-off; characterization fixtures; independent cursors; reversible database additions                     | Legacy and Fabric simulator suites pass               |
| R-003 | Fabric and legacy completion paths display twice                             | High       | Medium   | Correlation-bound Fabric delivery state; suppress only the exact acknowledged legacy completion                                   | Duplicate/restart tests for G2 and Meta               |
| R-004 | Durable retries repeat an agent prompt                                       | Medium     | High     | One idempotency identity across Agent Mesh outbox, bridge, CIT event, flow, and prompt result; durable claims on both sides       | Restart and duplicate-delivery contract tests         |
| R-005 | Retried physical command repeats movement                                    | Medium     | Critical | TTL, idempotency ledger, exclusive lease, adapter duplicate cache, local watchdog; movement excluded from replay/outbox           | Fault injection and hardware-safe simulator evidence  |
| R-006 | Bridge credential gains excessive Agent Mesh or device authority             | Medium     | Critical | Dedicated service identities, explicit scopes, no admin/arm/movement/approval grant, hash-only token storage, expiry/revocation   | Negative authorization matrix and audit records       |
| R-007 | Console auth is mistaken for loopback trust                                  | Medium     | High     | Deny-by-default Fabric router, independent CIT tokens, same-origin serving, exact WebSocket origin, no URL/browser storage tokens | Unauthorized/cross-origin tests and header inspection |
| R-008 | Raw audio, images, video, EEG, secrets, or CLI frames enter semantic storage | Medium     | High     | Data-class allowlist, bounded schemas, redaction, payload limits, raw-stream exclusion, retention defaults                        | Privacy fixture and forbidden-field tests             |
| R-009 | MindWave value is presented as objective cognition                           | Medium     | High     | Preserve `mindwave.esense.*`; explicit calibration/derived recipe naming; instructor guidance                                     | Schema, UI labels, and course review                  |
| R-010 | Brain2Devices dependency/runtime conflicts destabilize core                  | High       | Medium   | Run as out-of-process adapter in its supported Python; versioned wire contract                                                    | Independent process contract test on Windows          |
| R-011 | Adapter crash or heartbeat loss leaves a device active                       | Medium     | Critical | Local watchdog and stop handler, node lease expiry, automatic disarm, process supervision, visible unsafe/unavailable state       | Crash/network-loss fault tests                        |
| R-012 | Agent Mesh schema changes break bridge                                       | Medium     | Medium   | Explicit version negotiation, tolerant read of documented optional fields, fail closed on unsupported major                       | Cross-repository contract fixtures                    |
| R-013 | Capability names imply substitution where semantics differ                   | Medium     | High     | Constraint/unit/version descriptors; vendor namespace for nonportable behavior; course validation                                 | RoboMaster/LEGO conformance tests                     |
| R-014 | A flow recipe becomes an arbitrary-code channel                              | Low        | Critical | Declarative allowlisted operators only; no eval, scripts, shell, dynamic imports, or raw LLM execution                            | Malicious recipe rejection tests                      |
| R-015 | WebSocket abuse exhausts local runtime                                       | Medium     | Medium   | Authentication timeout, frame/queue/rate limits, idle/heartbeat close, origin check, one active node connection                   | Load and malformed-frame tests                        |
| R-016 | Classroom network latency misses interactive targets                         | Medium     | Medium   | Semantic edge reduction, local routing, monotonic timestamps, rate declaration, p95 instrumentation                               | Local p95 measurements and dropped-event counters     |
| R-017 | Hardware/firmware assumptions are stale                                      | High       | High     | Simulator-first CI, separately tagged HIL runbooks, explicit firmware/pairing assumptions, no automatic discovery claims          | Signed/manual hardware validation record              |
| R-018 | LEGO's real transport split is incomplete and no Tuya adapter exists         | High       | Medium   | Keep LEGO simulation/default dependencies; implement ADR-023 transport split before BLE HIL; require Tuya discovery first         | Transport licence gate and adapter proposal           |
| R-019 | Pre-reconciliation work is lost during upstream adoption                     | Low        | High     | Immutable local snapshot `db29f15`, separate reconciliation branch/worktree, targeted porting, clean-base gates                   | Backup ref and final tree comparison                  |
| R-020 | Safety logic is split inconsistently between core and adapter                | Medium     | Critical | Core deterministic policy is authoritative; adapter repeats device bounds and safe-state behavior; contract tests require both    | Dual-denial and adapter-boundary tests                |
| R-021 | Hardware replay activates a device                                           | Low        | Critical | Replay API is dry-run by contract; hardware mode absent until separately approved and gated                                       | Replay tests prove zero dispatch                      |
| R-022 | Mobile/vendor glasses APIs cannot support desired acknowledgement            | Medium     | Medium   | Acknowledge at Agent Mesh bridge/service boundary; retain device-specific cursors; document last-mile semantics                   | Disconnect/restart simulator and manual HIL results   |
| R-023 | Operators confuse upstream classroom auth with standalone Fabric credentials | Medium     | High     | Separate process/database/port, explicit console login, independent token namespace, distinct runbook and health endpoint         | Cross-service token rejection and operator dry run    |

## Open assumptions requiring owner or hardware evidence

- Which RoboMaster checkout is the long-term supported vendor boundary.
- Whether the approved LEGO optional transport split works with the owner hub and Windows Bluetooth stack.
- Whether a Tuya integration must use LAN control, vendor cloud, or both.
- Exact G2 production plugin network and background-execution limits.
- Exact Meta Ray-Ban features available through the deployed Android/vendor stack.
- Classroom site/room identity provider strategy beyond first scoped local tokens.
