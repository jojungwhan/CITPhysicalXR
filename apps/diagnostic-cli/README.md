# Diagnostic CLI placeholder

Local diagnostics will be added with the runtime milestones. This directory exposes no shell, arbitrary process runner, device transport, or public service in Milestone 0.
